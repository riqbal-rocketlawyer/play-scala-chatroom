apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/darkn3rd/play-scala-chatroom-example:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/darkn3rd/play-scala-chatroom-example:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: play-scala-chatroom-example
        chartPath: charts/play-scala-chatroom-example
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/darkn3rd/play-scala-chatroom-example"
          image.tag: "{{.DIGEST_HEX}}"
